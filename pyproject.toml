[project]
name = "platform-pulse"
version = "0.1.0"
description = "A Streamlit app for Platform Pulse."
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.31.0",
    "pandas>=2.2.0",
    "altair>=5.2.0",
    "pytest>=8.0.0",
]

